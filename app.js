'use strict';

/**
 * Lando app-level plugin hook for lando-drupal-tools.
 * Tooling registration is handled in index.js via post-bootstrap-app.
 * This file is kept as a no-op to allow future app-level hooks if needed.
 */
module.exports = (app, lando) => {}; // eslint-disable-line no-unused-vars
